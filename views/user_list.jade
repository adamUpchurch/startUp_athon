extends layout

block content
    h1= title

    ul
        each founder in founder_list
            li
                a(href=founder.url) #{founder.name}
                |   - #{founder.description}
        else
            li There are no founders.